要理解的内容

- 理解包含不同的层次节点的DOM
- 使用不同的节点类型
- 克服浏览器兼容性问题及各种陷阱

**DOM是针对HTML和XML文档的一个API 描绘了一个层次化的节点树**



## 1.节点层次

 节点分为几种不同的类型，每种类型分别表示文档中不同的信息或标记

- 每个节点都拥有各自的特点、数据和方法.

**文档元素，文档元素是文档的最外层元素，文档中的其他所有元素都包含在文档元素中.**

每个文档只有一个文档元素.

文档中 12种节点类型

![1233](C:\Users\杨光华\Desktop\1233.gif)



1.Node类型











![111](C:\Users\杨光华\Desktop\111.webp)

 